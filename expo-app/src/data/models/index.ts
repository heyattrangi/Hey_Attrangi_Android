/**
 * Domain models used by stores & UI.
 * Alias of `types/domain` so data layer imports stay intentional.
 */
export type {
  AuthSession,
  AuthUser,
  LoginCredentials,
  SignUpPayload,
  PersonalInfo,
  ProfileBundle,
  Therapist,
  MoodLogEntry,
  CreateMoodLogInput,
  Session,
  BookingRecord,
  ChatMessage,
  ChatConversation,
  AppNotification,
  NotificationSettings,
  JournalEntry,
  WellnessModule,
} from '../../types/domain';
