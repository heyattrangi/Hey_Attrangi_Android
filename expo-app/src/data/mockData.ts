/** @deprecated Import from src/mocks instead. Kept for transitional compatibility. */
export {
  mockTherapists as MOCK_THERAPISTS,
  mockSessions as MOCK_SESSIONS,
  mockCalendarDays as CALENDAR_DAYS,
  mockTimeSlots as TIME_SLOTS,
  mockAiModes as AI_MODES,
  mockChatMessages as MOCK_CHAT_MESSAGES,
  mockMoodTrackerOptions as MOOD_TRACKER_MOODS,
  mockMoodTags as MOOD_TAGS,
  mockHomeMoods as HOME_MOODS,
  mockMoodHistoryChart as MOOD_HISTORY,
  mockDevices as MOCK_DEVICES,
  mockInvoices as MOCK_INVOICES,
} from '../mocks';
