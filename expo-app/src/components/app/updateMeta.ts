import { APP_VERSION } from '../../constants/appMeta';

export { APP_VERSION };

/** Store / Play listing placeholder */
export function envLink(): string {
  return 'https://heyattrangi.app/download';
}
